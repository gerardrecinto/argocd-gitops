# Staging cluster overrides

cluster:
  name: staging
  env: staging
  server: PLACEHOLDER_STAGING_CLUSTER_API

ingress:
  domain: PLACEHOLDER_STAGING_DOMAIN
  ip: PLACEHOLDER_STAGING_INGRESS_IP

monitoring:
  retention: 7d
  storage: 20Gi

replicas:
  default: 1
  min: 1
  max: 3
